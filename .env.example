# Environment Configuration for Expense Tracker

# Flask Secret Key (change this in production!)
SECRET_KEY=dev-secret-key-change-in-production

# Google OAuth Configuration (optional for local testing)
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# AI Configuration
# Set to "false" to use real Mistral AI API (requires API key)
# Set to "true" for local testing with mock responses
AI_MOCK_MODE=true

# Database Configuration (SQLite for local development)
# For production, use PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost/expense_tracker
